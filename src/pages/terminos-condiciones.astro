---
import { getEntry } from 'astro:content';
import PaginaLayout from '@/layouts/PaginaLayout.astro';

const entry = await getEntry('paginas', 'es/terminos-condiciones');
if (!entry) throw new Error('Missing paginas entry: es/terminos-condiciones');
---

<PaginaLayout entry={entry} showCta={false} />
