# Workers static-assets redirects — RELATIVE paths only.
# (Unlike Pages, Workers _redirects does not allow host/absolute-URL rules;
# the deploy fails with "Only relative URLs are allowed" if you add one.)
#
# Host-level protections live elsewhere:
# - workers.dev + preview URLs: disabled in wrangler.jsonc (workers_dev/preview_urls)
# - www.aletopintores.com -> apex 301: Cloudflare zone Redirect Rule
#   (Dashboard -> aletopintores.com -> Rules -> "Redirect from WWW to Root" template)
#
# Add path redirects below when URLs change, e.g.:
# /old-path/ /new-path/ 301
