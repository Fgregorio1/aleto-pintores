# Workers static-assets redirects.
#
# www -> apex: www.aletopintores.com is attached to this worker as a custom
# domain purely so this rule can 301 it to the canonical host.
https://www.aletopintores.com/* https://aletopintores.com/:splat 301

# Host-level protections elsewhere:
# - workers.dev + preview URLs: disabled in wrangler.jsonc (workers_dev/preview_urls)
#
# Add path redirects below when URLs change, e.g.:
# /old-path/ /new-path/ 301
